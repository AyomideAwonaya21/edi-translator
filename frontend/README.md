# Frontend (React + Vite)
Run locally:

```bash
cd frontend
npm install
npm run dev
```

By default it expects the backend at `http://localhost:8000`.
You can change `API_BASE` inside `src/api.js` if needed.
